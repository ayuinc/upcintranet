$multi_curl->addGet('https://webservice.upc.edu.pe/api/FNT_WSentMatricula/U/201214865');


ListaDTOMatricula
MatriculaCodPeriodMat

MatriculaCodAlumno


$current .= $instance->response->DTOHeader->CodigoRetorno;
for ($i = 0; $i<count($instance->response->ListaDTOHorarioAlumno); $i++){

$current .= "SECCION:".$instance->response->ListaDTOHorarioAlumno[$i]->SesionSECCION;

})r