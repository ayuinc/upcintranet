$(document).ready(function(){
	$('#init-date').datepicker({
		 format: "yyyymmdd"
	});

	$('#end-date').datepicker({
		 format: "yyyymmdd"
	});

	$('#init-date-formulario').datepicker({
		 format: "yyyymmdd"
	});
});